<%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, "标题" %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.text_area :text %>
  </div>
  <div class="actions">
		<div id="submit-cont">
        <%= f.submit "确认发布" %>
				<div class="link-h"><%= link_to "返回", home_index_path %></div>
		</div>
  </div>
<% end %>
